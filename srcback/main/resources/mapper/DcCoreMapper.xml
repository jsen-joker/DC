<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dryork.mapper.DcCoreMapper">

    <!-- 开启二级缓存 -->
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

    <insert id="sqlCUD" parameterType="java.lang.String">
        <![CDATA[ ${sql} ]]>
    </insert>

    <select id="sqlList" parameterType="java.lang.String" resultType="java.util.Map">
        <![CDATA[ ${sql} ]]>
    </select>

    <select id="sqlOne" parameterType="java.lang.String" resultType="java.util.Map">
        <![CDATA[ ${sql} ]]>
    </select>
</mapper>
